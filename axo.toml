project_name = "sunshine"

[tasks]
build = { command = "swift build", description = "Build the Sunshine package" }
test = { command = "swift test", description = "Run the Sunshine test suite" }
clean = { command = "swift package clean", description = "Remove build artifacts" }
